

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {

        domains: [
            "localhost",
            "asset-hub-five.vercel.app"
        ]

    }
}


module.exports = nextConfig
